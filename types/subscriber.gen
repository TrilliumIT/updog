package types

import (
	"github.com/cheekybits/genny/generic"
	"time"
)

//"SubscriberType=Applications,Application,Service"
type SubscriberType generic.Type

func (s *SubscriberType) Sub(full bool, depth uint8, maxStale time.Duration, onlyChanges bool) Subscription {
	return s.Subscribe(full, depth, maxStale, onlyChanges)
}

// vim: filetype=go
