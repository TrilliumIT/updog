package types

import (
	"github.com/cheekybits/genny/generic"
	"time"
)

//"Multi=Applications,Application,Service"
type Multi generic.Type

func (m *Multi) Sub(full bool, depth uint8, maxStale time.Duration, onlyChanges bool) Subscription {
	return m.Subscribe(full, depth, maxStale, onlyChanges)
}

// vim: filetype=go
